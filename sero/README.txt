Drop your profile picture in this folder named:  sero.png

It will show up as the pixelated desktop icon in the top-left,
and inside the window that opens when you click it.

(Square images look best — e.g. 256x256.)
